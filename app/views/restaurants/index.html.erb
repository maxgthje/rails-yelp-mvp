<h1>
All the restaurants</h1>

<p>
<ul>
<% @restaurants.each do |restaurant| %>
    <li>
      <%= restaurant.name %> | <%= restaurant.address %> | <%= restaurant.phone_number %> | <%= restaurant.category.capitalize %> |
      <%= link_to "See details", restaurant_path(restaurant.id) %>
    </li>
  <% end %>
</ul>
  <%= link_to "Create new Restaurant", new_restaurant_path %>
</p>
